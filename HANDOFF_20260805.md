# ELLMTrees paper handoff — 2026-08-05

## Start here next session

The next large task is to reconcile and repair
`WeightTraits_all_results_reproducibility.xlsx` against the current paper and the live result artifacts.
Do not begin by changing cells. First fetch `main`, inventory the workbook, and build a cell-level
reconciliation ledger as described below.

The remote paper branch was current at:

- `6bc1fe0` — merge of today's recovery-analysis, appendix, and LayerTrace work;
- `f002637` — subsequent Overleaf update; and
- `2b22ec2` — merge of that Overleaf update into `main`.

Always fetch before editing. Another Overleaf sync may have advanced `main` again.

## Work completed today

### Manuscript cleanup and corrected controlled results

- Removed the naive sentence-embedding baseline sentence from the behavior-only comparison and its
  appendix method sentence.
- Confirmed that the old LayerTrace values came from the historical `runs_branching_v3` cohort rather
  than the fresh shared-tree Flan simulations.
- Rescored the saved fresh Flan full-fine-tuning per-tensor distance cubes on the 46 topology-eligible
  trees. The cubes are under
  `../WeightTraits/outputs/analysis_v20260713/full_finetune/*/model_leaf_analysis/direct_distance_layers.npz`.
- Updated the LayerTrace subset table to the fresh matched sample:

| Subset | Tensors | Clade recovery (SE) | PAER (SE) |
|---|---:|---:|---:|
| full | 282 | 95.5% (2.0%) | 89.1% (4.6%) |
| high_signal | 48 | 94.9% (2.0%) | 87.0% (5.0%) |
| sak | 24 | 95.5% (2.0%) | 89.1% (4.6%) |
| encoder_h | 24 | 94.9% (2.0%) | 87.0% (5.0%) |
| enc_sak | 12 | 95.5% (2.0%) | 89.1% (4.6%) |
| low_signal | 36 | 93.5% (2.2%) | 82.6% (5.7%) |

- Removed the sentence retaining five historical decoder-only robustness groups.
- Removed the entire historical behavior-only Appendix K and every `tab:phylolm` / `app:phylolm`
  cross-reference.
- Removed the old translation-held-out row from the main recovery table.
- Kept only the fresh controlled PhyloLM comparison in the main table: Llama rank 8, rank 64, and full
  fine-tuning on the common 46 eligible truth trees.
- Updated the controlled PAER range from 70--94% to 70--93%.
- Fixed the reintroduced `Subsetes` typo during the merge with the latest Overleaf edits.

### LayerTrace figure

- Rebuilt `figures/fig3_layertrace_grid.pdf` and its PNG preview.
- The Flan panel now uses the fresh 46-tree full-fine-tuning sample. The Llama panel remains the earlier
  47-run archive.
- The fresh per-tensor interpretation is different from the historical plot: Flan key matrices average
  6.8% FN and all other tensors average 7.0% FN, both rounding to 7%. The signal is diffuse at the
  individual-tensor level. This is compatible with the sparse aggregate result: averaging only the 12
  encoder self-attention key matrices still matches the all-weight reconstruction.
- The older Llama panel remains more key-concentrated: 13% FN for key matrices versus 20% elsewhere.
- Updated the appendix sample accounting, prose, caption, and `REPRODUCIBILITY.md` accordingly.

The fresh profile was computed with a temporary local script at
`tmp/pdfs/build_fresh_layertrace.py`; that file was not committed. Before a future full figure rebuild,
promote this logic into a checked-in reproducibility script. Avoid editing the already-dirty
`../ELLMTrees/scripts/make_layertrace_grid.py` in place without first inspecting the user's changes.

### Git and validation

- The paper changes were merged directly to `main` and pushed.
- The final merge preserved newer Overleaf prose while retaining the corrected values and requested
  deletions.
- `git diff --check` passed; LaTeX environment counts were balanced; stale Appendix K references,
  historical-cohort text, old `n=44` LayerTrace labels, and the naive baseline sentence were absent.
- The refreshed LayerTrace PDF was rendered and visually inspected. It is a one-page 514 x 291 point
  vector PDF.
- The manuscript was not compiled locally because this machine has no `pdflatex`. Overleaf remains the
  authoritative build.

### Dirty-worktree warning

The primary checkout was intentionally left on `agent/figure2-recovery-logit` with two unrelated local
figure modifications:

- `figures/hf_mistral_hero_3panel.pdf`
- `figures/regression_diagnostics_semantic.pdf`

Do not reset, discard, stage, or overwrite those files. Do not use `git add -A` in any of the three
repositories; all have substantial unrelated work.

## Workbook audit snapshot

Primary workbook:

- `WeightTraits_all_results_reproducibility.xlsx`
- 361,738 bytes when inspected on 2026-08-05
- 26 sheets
- approximately 4,200 formulas
- no defined names and no formal Excel tables

Companion audit workbook:

- `outputs/iclr_v4_claim_audit_20260803/iclr_v4_claim_audit.xlsx`
- 5 sheets: Summary, Claim Ledger, Findings, Evidence Sources, Status Definitions
- useful as a starting ledger, but it predates today's changes and must not be treated as current truth

The main workbook has a sensible dependency structure:

1. raw/source sheets (`T2 Recovery Raw`, `T2 Ordering Raw`, `Bridge DL Calc`, `T6 Coefficient Raw`,
   `T9 Raw`, `T10 Behavior Raw`, `T10 Weight Raw`, depth sheets);
2. calculated summary sheets (`T2 Recovery`, `Bridge Summary`, `T4 Layers`, `T5 LoRA`, `T6 Regression`,
   `T7 Heterogeneity`, `T9 Within Task`, `T10 PhyloLM`, `Empty Sensitivity`);
3. paper-facing control sheets (`README`, `Paper Update`, `Claim Index`, `Source Manifest`,
   `Reproduction Commands`, `T2 Shared Samples`, `T10 Shared PhyloLM`).

## Known workbook/paper differences already identified

These are examples, not an exhaustive diff:

- `README` still names `iclr_draft_v2.tex`, describes a numeric-only revision, and says the workbook
  covers Tables 2--10. The live paper is `iclr_draft_v4.tex`, and the edits are no longer numeric-only.
- `Paper Update` is an August 3 numeric-substitution log tied to old workbook cells, not a current
  paper-to-source reconciliation.
- `Claim Index` still reports clade 82--97% and PAER 72--94%, uses old table numbering, and describes
  the historical five-row PhyloLM table that was deleted today.
- `T2 Recovery` is driven by historical row ranges and mixed samples (for example, 40--47 rows for
  several Flan/Llama conditions and 50 rows for Llama full FT). The paper now reports the common 46
  topology-eligible trees for recovery and 26 trees for ordering in every fresh shared-tree row.
- `T2 Shared Samples` currently counts all 50 generated trees as recovery observations and contains
  older LoRA ordering/recovery values. Four truths (015, 020, 037, 047) have no nontrivial unrooted
  split and must be excluded from topology metrics, leaving 46. It also retains the removed earlier
  translation-held-out row.
- `T4 Layers` contains tensor definitions and percentages but not the fresh 46-tree recovery and PAER
  values listed above.
- `T10 PhyloLM` and `T10 Shared PhyloLM` are built entirely around the historical five-group cohort
  (`n=47,47,47,44,46`). That cohort and Appendix K no longer appear in the paper. The workbook needs a
  clearly labeled archive or deletion policy plus a new fresh three-row controlled comparison.
- `Source Manifest` hashes old v2 paper files and older aggregate CSVs. It does not yet enumerate the
  fresh WeightTraits rollups, saved distance cubes, fresh PhyloLM metrics, Figure 2 summary/record files,
  or the current v4 paper hash.
- `Reproduction Commands` points to the August 3 workbook builder and legacy result commands. It must be
  updated to the current generators and exact eligibility filters.
- Formula caches currently contain old results. Editing formulas with `openpyxl` will not recalculate
  them. The bundled `soffice` executable is available and should be used for a full recalculation pass
  before validating `data_only=True` values.

## Approach for the workbook repair

### Phase 1: freeze and inventory

1. Fetch remote `main` and record the exact paper commit used for reconciliation.
2. Hash the original workbook and make a dated backup before changing it.
3. Inventory every sheet's dimensions, formulas, merged cells, hidden rows/columns, freeze panes,
   filters, widths, number formats, conditional formatting, images/charts, and print settings.
4. Save a formula map and a style map. This makes accidental formula replacement or formatting damage
   detectable.
5. Re-run the existing claim-audit extraction against the fetched v4 source so the starting ledger is
   current.

### Phase 2: establish source-of-truth rules

Use this precedence:

1. per-run raw artifacts and locked manifests;
2. checked-in analysis/rollup scripts and their generated CSV/JSON summaries;
3. verified manuscript tables and figures;
4. workbook cells.

The workbook is a reproducibility and presentation layer, not the authority when it disagrees with a
rerunnable result artifact. Every summary row must record:

- exact source path and hash;
- generation command;
- architecture/training/representation;
- generated-tree count;
- eligible-tree count and explicit excluded IDs;
- ordering count under its separate eligibility rule;
- metric definition, aggregation rule, and SE definition;
- whether the source is fresh shared-tree, historical archive, or organic-family evidence.

Do not combine historical and fresh cohorts in one summary range. Do not treat generated `n=50` as
topology-scored `n=46`.

### Phase 3: build a reconciliation ledger before editing

Generate a machine-readable ledger with one row per paper claim/table cell/figure statistic:

| Field | Purpose |
|---|---|
| paper location and quoted claim | identifies the consumer |
| workbook sheet/cell | identifies the current provider |
| raw source artifact | identifies the authority |
| paper value / workbook value / recomputed value | exposes the mismatch |
| sample IDs and `n` | catches cohort drift |
| metric and uncertainty definition | catches semantic drift |
| status | match, stale, sample mismatch, formula bug, label bug, source missing, intentional archive |
| proposed action | update raw input, repair formula, relabel/archive, update paper, or investigate |

Start from the August 3 claim ledger, but extend it to cell-level mappings and current table/figure
labels. Review the ledger before bulk workbook changes.

### Phase 4: repair in dependency order

1. Update `Source Manifest` and `Reproduction Commands` first.
2. Replace or append raw data blocks from current compact CSV/JSON sources. Preserve historical rows
   only in explicitly labeled archive sheets; do not let them feed live paper summaries.
3. Repair formulas and ranges in calculation sheets. Prefer formulas over pasted summary values.
4. Rebuild summary sheets from those formulas:
   - fresh shared-tree recovery/ordering;
   - fresh LayerTrace subsets and the refreshed per-tensor profile metadata;
   - current behavior bridge, heterogeneity, and empty-output sensitivity;
   - current within-task results;
   - fresh three-condition PhyloLM comparison;
   - Figure 2 diagnostics and paired-bootstrap odds ratios.
5. Update `Claim Index`, `Paper Update`, and `README` last, after all downstream values are stable.
6. Decide explicitly how to handle removed material. Recommended default: move the historical PhyloLM
   cohort and translation-held-out row to clearly named archive sheets, excluded from live formulas,
   rather than silently deleting provenance.

### Phase 5: recalculate and verify

1. Save an intermediate XLSX, run a full recalculation through bundled `soffice`, reopen it, and verify
   cached values with `data_only=True`.
2. Require zero `#REF!`, `#VALUE!`, `#DIV/0!`, or broken external links.
3. Compare every live workbook result to the reconciliation ledger with explicit tolerances. Counts and
   topology ID sets must match exactly; displayed rounded values must round identically to the paper.
4. Verify the common 46 topology IDs across all fresh rows and the common 26 ordering IDs separately.
5. Visually render every paper-facing/summary sheet and inspect clipping, widths, wrapping, legends,
   frozen headers, print areas, and number formats.
6. Re-run the manuscript claim audit from the recalculated workbook. If the workbook uncovers a genuine
   paper error, fix the paper from the raw source and record the reason; never make the workbook match by
   hiding a real discrepancy.
7. Produce a final before/after reconciliation report and a list of intentionally archived values.

## Expected deliverables next week

- Updated `WeightTraits_all_results_reproducibility.xlsx`.
- Dated untouched backup of the pre-repair workbook.
- Machine-readable reconciliation ledger (CSV or XLSX) with source paths and sample IDs.
- Updated claim-audit workbook.
- Short changelog listing repaired sheets, formula/range changes, archived historical rows, and any
  paper changes triggered by genuine source-level errors.
- Visual QA renders for the workbook's paper-facing sheets.

## Important operating constraints

- Use the spreadsheet skill and its render/verify workflow when the workbook edit begins.
- Do not edit the live workbook in place until the backup and inventory exist.
- Do not overwrite formulas with cached values.
- Do not infer eligibility from row counts; join by canonical topology ID and assert the exact set.
- Do not overwrite unrelated dirty files in `ELLMTrees`, `WeightTraits`, or `ELLMTrees-paper`.
- Do not push workbook or paper changes until the reconciliation ledger and visual QA both pass.
- Compile the paper on Overleaf after any workbook-driven manuscript change; local TeX is unavailable.

